import { MDText, MDView } from '@/components';

export default function Onboarding() {
  return (
    <MDView style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <MDText>Onboarding</MDText>
    </MDView>
  );
}
